Test sorting for refineries:
* How do refineries decide where to pull from?? Are they pulling from 
physically closest or some magical conveyor closest distance?
* Can we put ores into a container that is closest to all refineries?
* Can we put ores into containers in such a manner that they are 
refined to quota?

Tool tiering:
* Can we determine groups of tools and tiers within those groups?
* Some sort stat we can look at?

Tool swapping:
* Can we put just 1 tool of a kind in character inventories when
swapping?

Assemblers:
* Assemble the list of stuff that was reported missing from last /sort.